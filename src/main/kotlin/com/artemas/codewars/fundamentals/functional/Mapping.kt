package com.artemas.codewars.fundamentals.functional

class Mapping {
    companion object {
        fun addNumbers(listOfNumbers: List<Int>): Int {
            return listOfNumbers.sum()
        }

        fun subtractNumbers(listOfNumbers: List<Int>): Int {
            // TODO: Complete
            return listOfNumbers.get(0)
        }
    }
}